defaults:
  - pems04.yaml

experiment:
  name: pems04_full_semantic

dataset:
  dynamic_semantic:
    enabled: true
    strict_truncation: true

model:
  gamma: 0.35
  # ASSUMPTION: full setting uses paper-style semantic-guided initialization.

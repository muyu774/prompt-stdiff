defaults:
  - pems04.yaml

dataset:
  input_dim: 1
  history_steps: 12
  horizon_steps: 12

model:
  predict_residual: true
  gamma: 0.1

diffusion:
  clip_x0: true

train:
  lr: 2.0e-4
  metric_feature_index: 0

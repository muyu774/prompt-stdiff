defaults:
  - pems08_pdformer_resdiff_incident_tail.yaml

model:
  use_semantic: false
  hetero_scale_use_semantic: false
  incident_tail_use_semantic: false

train:
  save_dir: ./outputs/checkpoints/pems08_pdformer_resdiff_incident_tail_nosem

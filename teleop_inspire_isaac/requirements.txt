# Core dependencies for the teleop_inspire_isaac subproject.
# These run the mocap parsing, retargeting, pipeline and tests on CPU.
numpy>=1.24.0
PyYAML>=6.0

# Optional / hardware runtimes (install separately, not pulled by CI):
#   isaacgym            # NVIDIA Isaac Gym, requires a CUDA GPU
#   Noitom Axis SDK     # for the native Perception Neuron data stream
